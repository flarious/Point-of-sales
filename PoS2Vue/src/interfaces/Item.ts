export interface Item {
    id: number,
    code: string,
    name: string,
    price: number,
    unit: string,
    unit_id: number,
}

export interface ItemModel {
    code: string,
    name: string,
    price: number,
    unit_id: number,
}